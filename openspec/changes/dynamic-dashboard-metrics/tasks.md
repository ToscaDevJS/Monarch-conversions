# Tasks: Dynamic Dashboard Metrics

## Unit 1: Reactive Metric Calculation
- [x] Add `@Query` in `MetricsHeaderView.swift` to fetch `[ConversionRecord]`.
- [x] Calculate `totalProcessedCount`, `workingQueueCount`, `convertedTodayCount`, `storageSavedText`, and `activeProjectsCount`.
- [x] Render dynamic values in metric header cells.

## Unit 2: Verification & Unit Tests
- [x] Create `MetricsHeaderViewTests.swift` testing calculation helper logic.
- [x] Run `xcodebuild test` and produce `verify-report.md`.
