# Tasks: Keyboard Shortcuts & Native macOS Menu Bar Commands

## Unit 1: Scene-level Shortcuts in ConvertScene
- [x] Add `⌘O` shortcut to trigger file browsing in `ConvertScene.swift`.
- [x] Add `⌘R` / `⌘↵` shortcuts to trigger batch conversion in `ConvertScene.swift`.
- [x] Add `⌘⌫` shortcut to delete selected queue item in `ConvertScene.swift`.
- [x] Add `⌘K` shortcut to clear batch queue in `ConvertScene.swift`.

## Unit 2: Global Tab Navigation & Menu Bar Integration
- [x] Add `⌘1`, `⌘2`, `⌘3` shortcuts to switch tabs in `RootView.swift`.
- [x] Add native macOS menu commands (`.commands`) in `Monarch_conversionsApp.swift`.

## Unit 3: Verification & Tests
- [x] Verify test suite compilation and execution with `xcodebuild test`.
- [x] Produce `verify-report.md`.
