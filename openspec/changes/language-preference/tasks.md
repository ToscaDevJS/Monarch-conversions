# Tasks: Language Preference Setting

- [x] 1. Define `AppLanguage` enum & `UserSettings.language` property with `UserDefaults` backing
- [x] 2. Update `LanguagePanelView` with interactive selection control and VoiceOver support
- [x] 3. Inject `.environment(\.locale, ...)` in `RootView`
- [x] 4. Add unit test suite for `AppLanguage` and `UserSettings.language` persistence in `UserSettingsTests.swift`
