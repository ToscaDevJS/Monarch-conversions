# Implementation Tasks: Appearance Preference

- [x] 1. Update `UserSettings.swift` to add `UserDefaults` persistence and `colorScheme: ColorScheme?` helper
- [x] 2. Update `AppearancePanelView.swift` with proper VoiceOver accessibility labels and trait indicators
- [x] 3. Apply `.preferredColorScheme()` to root scene in `Monarch_conversionsApp.swift` or main scene view
- [x] 4. Verify UI switching between Dark, Light, and System preference options
