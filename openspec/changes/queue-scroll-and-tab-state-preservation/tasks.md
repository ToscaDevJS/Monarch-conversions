# Tasks: Queue Scroll View, Window Constraints & Tab State Preservation

## Unit 1: Strict TDD Test Suite (RED)
- [x] Create `Monarch-conversionsTests/QueueLayoutAndTabStateTests.swift`.
- [x] Run `xcodebuild test` and verify RED phase failure.

## Unit 2: Implementation (GREEN)
- [x] Add `ScrollView` inside `BatchQueueView.swift`.
- [x] Update `RootView.swift` to preserve scene instances with `ZStack`.
- [x] Update `Monarch_conversionsApp.swift` with `.frame(minWidth: 860, minHeight: 600)`.
- [x] Run `xcodebuild test` and verify GREEN phase pass.

## Unit 3: Verification & SDD
- [x] Run full test suite with 0 regressions.
- [x] Produce `verify-report.md` and complete SDD verification.
